import { Stack } from 'expo-router';

export default function MedicalCvLayout() {
    return (
        <Stack
            screenOptions={{
                headerShown: false,
            }}
        />
    );
}